<Window x:Class="LibraryApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:vm="clr-namespace:LibraryApp.ViewModels"
        xmlns:v="clr-namespace:LibraryApp.Views"
        
        mc:Ignorable="d"
        Title="Умная библиотека" Height="720" Width="1280">

    <ContentControl.Resources>
        <DataTemplate DataType="{x:Type vm:AIViewModel}">
            <v:AIView/>
        </DataTemplate>
    </ContentControl.Resources>

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="0,0,0,1">
            <Grid Height="60">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Умная библиотека" Grid.Column="0" 
                           VerticalAlignment="Center" HorizontalAlignment="Left"
                           Margin="20,0,0,0" FontSize="18" FontWeight="Bold"/>

                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="500" HorizontalAlignment="Center" VerticalAlignment="Center"
                         Background="#F0F0F0" Padding="5" />

                <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Button Content="📚 Библиотека" Command="{Binding NavigateCommand}" CommandParameter="Library" Margin="5"/>
                    <Button Content="🏷️ Теги" Command="{Binding NavigateCommand}" CommandParameter="Tags" Margin="5"/>
                    <Button Content="🤖 ИИ" Command="{Binding NavigateCommand}" CommandParameter="AI" Margin="5"/>
                    <Button Content="⚙️ Настройки" Command="{Binding NavigateCommand}" CommandParameter="Settings" Margin="5"/>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>

                <DataTemplate DataType="{x:Type vm:LibraryViewModel}">
                    <v:LibraryView/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type vm:AIViewModel}">
                    <v:AIView/>
                </DataTemplate>

            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>